<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".ui.activity.FormularioLivroActivity">

        <EditText
            android:id="@+id/activity_formulario_livro_titulo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Título"
            android:layout_margin="16dp"
            android:inputType="text"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <EditText
            android:id="@+id/activity_formulario_livro_autor"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Autor"
            android:inputType="text"
            app:layout_constraintEnd_toEndOf="@id/activity_formulario_livro_titulo"
            app:layout_constraintStart_toStartOf="@id/activity_formulario_livro_titulo"
            app:layout_constraintTop_toBottomOf="@id/activity_formulario_livro_titulo" />

        <EditText
            android:id="@+id/activity_formulario_livro_preco"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="Preço"
            android:inputType="numberDecimal"
            app:layout_constraintEnd_toEndOf="@id/activity_formulario_livro_autor"
            app:layout_constraintStart_toStartOf="@id/activity_formulario_livro_autor"
            app:layout_constraintTop_toBottomOf="@id/activity_formulario_livro_autor"/>

        <Button
            android:id="@+id/activity_formulario_livro_botao_salvar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Salvar"
            android:layout_margin="16dp"
            app:layout_constraintVertical_bias="1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/activity_formulario_livro_preco"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
